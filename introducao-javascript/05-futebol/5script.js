// variantes

const pontos = 40;
const saldoDeGols = 5;
const venceuUltimoJogo = true;
const classificado =
pontos>= 35 && (saldoDeGols > 0 || venceuUltimoJogo)
console classificado, log( classificado )
document.getElementById("resultado").textContent =
'Time classificado para a proxima fase? $(classificado)';
console.log(classificado);
document.getElementById("resultado")
.textcontent =
'time classificado para a proxima ' + 'fase? ${classificado}';