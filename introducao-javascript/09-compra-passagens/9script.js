// variantes
const nomeAluno = joao;
const mediaFinal = 7.2;
let situacao;
if (mediaFinal >= 7) {situacao = "aprovado"}; } else { situacao = "reprovado";}
document getElementById("resultado").textContent = '$(nomeAluno} - media $(mediaFinal.toFixed(1): $(situacao)';